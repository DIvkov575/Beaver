sources:
  pubsub_in:
    type: gcp_pubsub
    project: "neon-circle-400322"
    subscription: "input-sub-1"
    decoding:
      codec: "json"


sinks:
  pubsub_out:
    inputs:
      - "pubsub_in"
    type: gcp_pubsub
    project: "neon-circle-400322"
    topic: "sub-2"
    encoding:
      codec: "json"
  console_out:
    inputs:
      - "pubsub_in"
    type: console
    encoding:
      codec: "json"
